package ${packageName};

import org.openqa.selenium.support.FindBy;
import ru.autotestframework.ui_core.junit.InjectedPage;
import ru.autotestframework.ui_core.page_manager.Element;
import ru.autotestframework.ui_core.page_manager.PageEntry;
import ru.autotestframework.web_elements.elements.*;

/**
 * Page Object для страницы: ${pageInfo.pageName}
 * URL: ${pageInfo.url}
 * Сгенерировано: ${date}
 */
@PageEntry(title = "${className}")
public class ${className} extends InjectedPage<${className}> {
#foreach($element in ${pageInfo.elements})
    /**
     * Элемент: ${element.name}
     * Селектор: ${element.selector}
     * Тип: ${element.elementType}
     */
    ${element.elementAnnotation}
    ${element.findByAnnotation}
    public ${element.fieldType} ${element.fieldName};

#end
}